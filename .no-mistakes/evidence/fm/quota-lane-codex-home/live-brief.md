# Codex quota routing validation

Fix an off-by-one error in a small Bash function. The cause is already isolated; use the Codex work rule. This is a local dispatch dry run and does not launch an agent.
